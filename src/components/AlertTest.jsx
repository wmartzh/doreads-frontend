import Alert from '@mui/material/Alert';
import { styled } from '@mui/material/styles';

const AlertCompont = styled(Alert)({
  fontFamily: ['Open Sans', 'sans-serif'].join(','),
  width: '90%'
});

export default AlertCompont;
